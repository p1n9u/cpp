/* Declares objects that control reading from and writing to the standard streams. */
#include <iostream>

using namespace std;

int main(void)
{
    /* Specifies the cout global stream. */
    cout << "from sea to shining C\n";
    return 0;
}
